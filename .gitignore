# Only web files belong in this repo.
# Everything else is ignored.

# iOS / Swift
*.swift
*.xcodeproj/
*.xcworkspace/
*.pbxproj
DerivedData/
.derived-data-log-*
Pods/
*.resolved

# macOS
.DS_Store
._*

# Local dev Firebase config — contains real API key, never commit
firebase-config.js
